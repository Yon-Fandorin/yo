schema: methexis.source/v1alpha1
id: agent.model-001
revision: sha256:2eaa80395f65dbb097a8d961067854000e6c85c8325bed8ef7f73f9951f800e5
payload:
  kind: decision
  content: Routing uses stable ProviderId, AccountId, and ModelId with one durable connections.yaml owner for external accounts, catalog and OpenRouter discovery seeds, complete model definitions, preference, and warning-only per-model last_failure observations; config.yaml owns no model state. User-authored grouped definitions enter only through explicit bounded yo connect --from with an absolute file path or exact stdin marker -, while credential-file paths are absolute. One document carries one Provider-and-Account credential scope and either bounded multiple models, a closed catalog, or the exact zero-model OpenRouter discovery-seed form, contains no secret, fails atomically on any invalid or duplicate member, and commits all-or-nothing after one preview and confirmation. Re-import replaces that pair's whole definition group rather than merging, preserves observations only for exact unchanged bindings, and never invents a preference; a removed preferred target is cleared. The profile algebra keeps explicit endpoint and closed whole-field overrides but removes connection-only verification_profile. A complete profile may carry a positive max_output_tokens as a known hard maximum; absence records that Yo does not know a trustworthy numeric maximum and a compatible Connector must omit its output-limit field. The agent runtime never guesses a number; for a known maximum it may select a smaller positive request-local cap without changing durable binding, replay identity, or binding epoch. Presence, absence, and the known number participate in exact complete-binding equality. External connect performs no model request and proves no authentication, entitlement, request acceptance, or semantic terminal; an explicitly chosen dynamic catalog may perform only its bounded inventory request. Actual model use reports a closed typed outcome and the exact private CredentialRevision used or observed without holding the repository lock during the remote request. A brief operation-locked writer first resolves any pending operation journal, then re-reads both repositories and CAS-records last_failure only when the same exact current binding and CredentialRevision remain current, while success clears it under the same fence; unresolved recovery skips observation mutation and removed, replaced, or credential-stale outcomes are discarded. Observation state never changes binding identity or routing, and cancellation, local tool failure, cleanup failure, secrets, request bodies, response bodies, and raw provider errors are excluded. Credential/public connect and disconnect remain recoverable through the secret-free journal, while preference and observation writes remain single-repository CAS operations. No manual/managed provenance, model-config composition, legacy decoder, migration, dual write, dedicated retired-shape classifier, or versioned compatibility branch remains.
