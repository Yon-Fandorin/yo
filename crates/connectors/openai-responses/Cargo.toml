[package]
name = "yo-connector-openai-responses"
version = "0.0.0"
edition.workspace = true
repository.workspace = true
publish = false

[dependencies]
reqwest = { version = "=0.13.4", default-features = false, features = ["json", "rustls", "stream"] }
serde_json = "1.0.140"
yo-connector-transport = { path = "../transport" }
yo-core = { path = "../../yo-core" }

[dev-dependencies]
yo-connector-transport = { path = "../transport", features = ["test-support"] }

[lints]
workspace = true
